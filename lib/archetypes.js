/**
 * Canonical 12 archetypes in solar-season order (0°–30° = Ignispectrum, etc.).
 * Re-exports from contract — single source of truth for lib/ and components.
 */

export { LIGS_ARCHETYPES } from "@/src/ligs/archetypes/contract";
